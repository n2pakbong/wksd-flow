[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "wksdflow"
version = "0.1.0"
dependencies = ["jax", "numpy", "matplotlib"]

[tool.setuptools]
packages = ["wksdflow"]
